# One exhaustive epoch over the deterministic task-balanced 10% RealSource
# view.  This is the production Stage-A population for curriculum v2: every
# selected row is consumed once, all 35 tasks remain represented, and the
# immutable RealSource holdout is excluded.
extends:
  - realsource_common_v1.yaml

run_id: robot_ft_realsource_production10_18d_delta_h100x8_b16
canonical_eval_split_id: realsource_strict_valid_fractional_eval128_v4

datasets:
  vla_data:
    canonical_eval_manifest: /data/mehul-vla-jepa/checkpoints/eval_manifests/realsource_strict_valid_fractional_eval128_v4.json
    frozen_train_view_manifest: /data/mehul-vla-jepa/data_contracts/views/realsource_strict_valid_balanced_10_v1.json
    frozen_train_view_manifest_sha256: cdf2046b8242621e34c715615d58fe38203dd17368ff738229ef0657b943061c

trainer:
  # Four equally spaced checkpoint-backed evaluations over the only
  # RealSource pass.  The natural-final checkpoint is always the handoff.
  checkpoint_eval_milestone_fractions: [0.25, 0.5, 0.75, 1.0]
